Review current changes and update specs. Specs are and should be defined defined in `specs/**/*.md` and `specs/*.md`.

1. Review current changes in branch and/or bead
2. Analyse which specs might be impacted and if new specs need to be created (e.g. model, controller, component, page, etc.)
3. Create a plan which specs to update
4. Apply changes
5. Generate a report table of the updates. Table should have 2 comuns: Spec, Overview (example: "frontend/components/Markdown.md", "added className property")